# FastAPI + Celery 任务链学习案例

这是一个简洁的学习案例，展示如何使用 FastAPI 和 Celery 实现解耦的任务链处理。

## 核心概念

### 1. 任务链 (Task Chain)
将复杂任务分解为多个独立的处理步骤，每个步骤可以独立测试和复用。

### 2. 解耦设计 (Decoupled Design)
- **处理器模式**: 每个处理步骤都是独立的处理器
- **上下文传递**: 通过统一的上下文对象传递数据
- **管道模式**: 将多个处理器组合成处理管道

### 3. 异步任务队列
使用 Celery 实现任务的异步执行和监控。

## 快速开始

1. 启动 Redis: `redis-server`
2. 安装依赖: `pip install -r requirements.txt`
3. 启动服务: `./start.sh`
4. 访问文档: http://localhost:8000/docs

## 学习要点

### API 层 (`main.py`)
- 请求接收和响应
- 异常处理
- 路由组织

### 任务层 (`app/tasks/`)
- Celery 任务定义
- 异步任务执行
- 结果返回

### 处理器层 (`app/processors/`)
- 单一职责原则
- 可组合的处理单元
- 统一的接口设计

### 配置层 (`app/core/`)
- 配置管理
- 日志配置
- Celery 应用配置

## 扩展方向

1. 添加新的处理器
2. 实现不同的任务链组合
3. 添加任务监控和重试机制
4. 集成数据库存储
